{% extends '@BelousProducts/base.html.twig' %}
{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}

    <input type="submit" class="btn" value="Create"/>
    {{ form_end(form) }}

    <a href="{{ path('product_list') }}">{{ 'return_to_list' | trans }}</a>
{% endblock %}